# ComfyUI-4K4D Dependencies
# NOTE: PyTorch, CUDA, and ComfyUI are NOT listed here — they are assumed present.
# CUDA extensions (tinycudann, diff-point-rasterization) are installed by Node 2.

jinja2>=3.1.0
pyyaml>=6.0
psutil>=5.9.0
pillow>=9.0.0
numpy>=1.24.0
scipy>=1.10.0
gdown>=4.6.0
ffmpeg-python>=0.2.0
