# Auto-generated by ComfyUI-4K4D — Dataset Configuration
# Dataset: {{ dataset_name }}

dataloader_cfg: &dataloader_cfg
    dataset_cfg: &dataset_cfg
        data_root: {{ data_root }}
        images_dir: {{ images_dir | default('images') }}
        {% if masks_dir %}
        masks_dir: {{ masks_dir }}
        {% endif %}
        ratio: {{ ratio | default(0.5) }}
        bounds: {{ bounds | default('[[-5.0, -5.0, -5.0], [5.0, 5.0, 5.0]]') }}
        {% if force_sparse_view %}
        force_sparse_view: true
        {% endif %}
        view_sample: [{{ view_sample }}]
        frame_sample: [{{ frame_sample }}]

val_dataloader_cfg:
    dataset_cfg:
        <<: *dataset_cfg
